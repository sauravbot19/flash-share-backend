server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    # Since this IS the registry server, it shouldn't try to register with itself
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/